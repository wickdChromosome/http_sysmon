cmake_minimum_required(VERSION 2.8.12)
project(http_sysmon)

add_definitions("-std=c++11")

include(${CMAKE_BINARY_DIR}/conanbuildinfo.cmake)
conan_basic_setup()

add_executable(http_sysmon main.cpp)
target_link_libraries(http_sysmon ${CONAN_LIBS})
